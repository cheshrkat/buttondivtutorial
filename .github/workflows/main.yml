name: 'Run tests over demo solution'

runs-on: ubuntu-latest

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
  
jobs:
  demo-test:
    steps:
      - run: npm install
      - run: npm run testdemo
